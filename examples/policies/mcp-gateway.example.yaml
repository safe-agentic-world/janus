version: v1
rules:
  - id: allow-upstream-order-view
    action_type: mcp.call
    resource: mcp://retail/get_order_details
    decision: ALLOW
    principals: ["system"]
    agents: ["nomos"]
    environments: ["dev"]

  - id: require-approval-upstream-refund
    action_type: mcp.call
    resource: mcp://retail/request_refund
    decision: REQUIRE_APPROVAL
    principals: ["system"]
    agents: ["nomos"]
    environments: ["dev"]
    obligations:
      approval_scope_class: action_type_resource

  - id: deny-upstream-compensation
    action_type: mcp.call
    resource: mcp://retail/issue_compensation
    decision: DENY
    principals: ["system"]
    agents: ["nomos"]
    environments: ["dev"]
