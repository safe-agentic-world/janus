# Default Profiles Moved

The canonical default profile bundles now live in `../../../profiles/`.

Use `profiles/safe-dev.yaml`, `profiles/ci-strict.yaml`, or `profiles/prod-locked.yaml` from the repository root.
